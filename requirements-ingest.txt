# SentinelAI (vigilex) -- Ingestion Worker dependencies (lightweight)
# Used by: docker/Dockerfile.worker-ingest
# No torch/transformers -- this worker only calls APIs and writes to DB.

# Database
psycopg2-binary==2.9.9
pgvector==0.3.5

# HTTP client (openFDA API)
httpx==0.28.1
requests==2.32.5

# Data
pandas==2.2.3
numpy==1.26.4

# Config / utils
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# Logging
python-json-logger==2.0.7
