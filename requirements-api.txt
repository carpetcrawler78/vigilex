# SentinelAI (vigilex) -- API-only dependencies (minimal, no ML stack)
# Used exclusively by docker/Dockerfile.api
# Keep this lean -- the API does DB queries only, no embeddings or inference

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
psycopg2-binary==2.9.9
pgvector==0.3.5

# Validation / config
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Logging
python-json-logger==2.0.7
